/**
 * Report service — Phase 5 stub.
 *
 * Aggregates a startup's Claims + Findings into a single investment-
 * readiness report. The first version is a deterministic JSON render;
 * the second is a PDF export (the only thing the examiner actually
 * needs to see offline).
 *
 * In Phase 2 this file is intentionally empty.
 */

module.exports = {};
